# Telegram Bot Token from BotFather
BOT_TOKEN=your_telegram_bot_token_here

# --- PostgreSQL Database credentials ---
# These are used by docker-compose.yml to set up the database
# and construct the DATABASE_URL for the backend and bot services.
POSTGRES_USER=school_user
POSTGRES_PASSWORD=school_password
POSTGRES_DB=school_discipline_db

# --- JWT Settings ---
JWT_SECRET_KEY=a_very_secret_key_that_should_be_changed
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# --- Database URL (for local development WITHOUT Docker) ---
# If you run the backend or bot locally (not in Docker), you will need to
# uncomment and set this variable to point to your database.
# The Docker setup constructs this URL automatically from the variables above.
# DATABASE_URL=postgres://school_user:school_password@localhost:5432/school_discipline_db
