from aiogram import Router

router = Router()

# Placeholder for future admin-specific bot commands
# Example:
# @router.message(Command("stats"))
# async def get_stats(message: types.Message):
#     await message.answer("Here are some stats...")
